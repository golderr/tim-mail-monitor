# Supabase Schema

This folder is reserved for database migrations and seed files.

- `migrations/`: ordered SQL migrations
- `seed/`: local seed assets and helper SQL

Milestone 2 now includes the first real ingestion schema for:

- mailbox configuration
- thread records
- messages
- message recipients
- attachments
- sync runs

